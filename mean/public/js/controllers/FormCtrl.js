angular.module('FormCtrl', []).controller('FormController', function($scope, Form, Nerd, $location) {
    angular.element(document).ready(function () {

        /*$scope.returnObj=function () {

           //console.log($scope.nerdObj);
            Form.create($scope.Nerd).then(function() {
                console.log($scope.Nerd);
            });
        };*/
    });
});
